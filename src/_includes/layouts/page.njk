---
layout: layouts/base.njk
---

{% if title %}
<div class="hero hero-solo">
  <div class="hero-text">
    {% if eyebrow %}<p class="eyebrow">{{ eyebrow }}</p>{% endif %}
    <h1>{{ title }}</h1>
  </div>
</div>
{% endif %}

{{ content | safe }}
